# METRON Frontend

React + Vite decision dashboard for the METRON retail pricing intelligence backend.

## Run

```powershell
npm install
npm run dev
```

Open `http://localhost:5173`.

The API defaults to `http://127.0.0.1:8000` and can be overridden with `VITE_API_BASE`.
